# Secrets
.env
PADDLE_TOKEN
apikey.txt

# Python files only — ignore DB, uploads, caches
pinkcloud.db
flywheel.db
flywheel.db-*
flywheel-local.db*
uploads/
__pycache__/
*.pyc
.venv/
uvicorn.log

# Generated OCR / repair artifacts (a few Sarvam fixtures under ocr_outputs/ stay tracked for tests)
repair/out/
repair/verify/
ocr_outputs/
raw/
