**What works (slice 3):** drag-and-drop or Browse of multi-file PDF/JPG/PNG/TIFF into `<div id="upload-root">`, one row per scan with live per-page status queued → uploading (progress bar) → processing → done/error driven entirely by a `MOCK` flag whose engine serves contract-shaped responses from `schema/doc_demo.json`; unsupported file types are rejected with a notice, rows are removable, and Start OCR stays disabled until a file is listed.
**How to test:** serve the repo (`python3 -m http.server`) and open any page that links `styles.css` + `upload.css` before `upload.js` and contains `<div id="upload-root">`; drop 3 files and click Start OCR — expect done/done/error (last page of a multi-file batch is the deliberate mock failure) and no console errors; when the FastAPI backend lands, change `const MOCK = true` to `false` in `upload.js` (one line) to hit `POST /jobs` + `GET /jobs/{id}`.
**Files touched:** `upload.js` (new), `upload.css` (new, scoped to `#upload-root`, tokens from `styles.css` only), `docs/handoff.md` (this file). No other file edited.
**Known gaps:** no host page exists in-repo (layout.html was removed on main — slice renders wherever `#upload-root` is mounted, which needs the layout slice/design-system pass); dropzone/progress-bar/spinner styles live scoped in `upload.css` and should migrate into the design-system `ui.css` per the layout plan's inventory note; a multi-page PDF counts as one row per file because page-splitting is backend-side (the poll adapter already consumes per-page `pages[]` results); mock error rule is deterministic (last page of a multi-file batch fails); a fallback copy of `doc_demo.json` is embedded in `upload.js` for `file://` previews where `fetch()` is blocked — keep it in sync.
**Slice 3 update (after tokens.css / ui.css / index.html / fastapi landed):** same upload screen, now wired to the moved design system — tokens.css `--pc-*` tokens only, ui.css components reused verbatim (`.btn--primary` Start OCR, `.btn--soft` Browse, `.btn--icon--ghost` delete, `.pill--tinted` status pills incl. `pill--default/processing/success/error`, `.toast--error` rejection notices, `.card` shells); scoped in upload.css only what ui.css lacks: dropzone, progress bar, processing spinner, file-row grid, and the orange "live scan" pill tint (`up-pill-live` on scan-active tokens). Real-backend path now matches the actual contract: one `file` per `POST /jobs` (job per file row), `GET /jobs/{job_id}` → `pending|done|error` with `result.pages[]`.
**How to test now:** open `index.html` over a static server — it already hosts `<div id="upload-root">` (page 1 slot) and links tokens.css/ui.css; add `<link rel="stylesheet" href="upload.css">` before `</head>` and `<script src="upload.js"></script>` before `</body>` (2 lines I could not add: task forbids me editing index.html) — then drop 3 files, click Start OCR, expect done/done/error with zero console errors; `MOCK=false` flips to the live FastAPI endpoints.
**Files touched (still only mine):** `upload.js`, `upload.css`, `docs/handoff.md` (appended). index.html untouched.
**Known gaps:** index.html does not link upload.css/upload.js yet — the screen only renders once those two tags are added (I need permission to edit index.html); progress bar still belongs in ui.css per the layout-plan inventory (ui.css shipped without one); a multi-page PDF is one row per file — backend splits pages inside `result.pages[]`, per-page rows need a later slice; mock failure rule stays deterministic (last page of a multi-file batch); `#upload-root.up-active` covers the viewport as page 1 until the router slice hands over.

Works: editor loads doc_demo.json offline, draws every line/word box on the scan, syncs clicks both ways, J/K walk the doubt queue, and the fix popup turns வாழறிவன் into வாலறிவன் on Accept (marks human + toast).
Test: serve this folder over http (python3 -m http.server), open editor.html, press J, then Enter, then Accept.
Touched: schema/schema.json, schema/doc_demo.json, tokens.css, ui.css, editor.css, index.html, api.js, translit.js, editor.js, scripts/smoke.sh.
Gaps: ./fonts/ is empty so Satoshi/Noto fall back; Export only toasts (page 3 is another slice); single mock page so J/K stay on page 1.

**Merge (slice/ui-editor-core -> main):** two pages. `index.html` is the product shell (page 1: `#upload-root` + upload.css/upload.js, inline `#doc` fallback kept) and the editor moved to `editor.html` (page 2, unchanged markup, IDs and JS). Styling comes from the re-skinned tokens.css/ui.css of the editor slice. ui.css ends with an "Upload screen compatibility" block that re-draws the old class names upload.js uses (`.btn--primary`, `.btn--icon`, `.card`, `.pill--tinted` + variants, `.pill__dot`, `.toast--error`) on the new system, and defines the `--ui-*` vars upload.css reads; tokens.css adds `--pc-color-neutral-200`, `--pc-track-h3`, `--pc-radius-pill`. upload.js/upload.css untouched. Schema + doc_demo.json kept from main (contract owner). Shell has an interim "Open editor" link (top right) until the router slice hands over.
**How to test now:** `python3 -m http.server`, open `/` (upload: drop 3 files, Start OCR, expect done/done/error), then "Open editor" or `/editor.html` (J, Enter, Accept).

**Works (pipe-export-receipt):** `GET /jobs/{id}/export.pdf` (visible HarfBuzz-shaped Tamil text pages first, then scan + invisible Tamil text layer per line; no receipt page; job id + SHA-256 in PDF Info), `/export.txt` and `/export.docx` (transcribed text only, no header or receipt section), `/receipt` (page count, auto vs human-review pages/lines, corrections by tier, reviewer, times, master SHA-256 re-verified on disk); real PaddleOCR scan exported and `pdftotext` returns the Tamil words (e.g. `பகவன்`, `வாலறிவன்`).
**How to test:** `uvicorn app.main:app`, `curl -F "file=@scan.jpg" localhost:8000/jobs`, then `curl -o out.pdf localhost:8000/jobs/<id>/export.pdf && pdftotext out.pdf - | grep பகவன்` and `curl localhost:8000/jobs/<id>/receipt?reviewer=Name`; `python -m pytest tests/test_export.py -v` (no paddle needed).
**Files:** `fastapi/sqlite/app/export.py` (new), `fastapi/sqlite/tests/test_export.py` (new), `fastapi/sqlite/app/main.py` (appended 3 routes only), `schema/endpoints.md` (appended Export section), `docs/handoff.md`. No new deps (pypdfium2 + Pillow).
**Known gaps:** no reviewer/corrections persistence in the backend yet, so reviewer comes from `?reviewer=` and correction counts stay 0 until the editor saves `corrections[]`/`verdicts[]`; text layer is unshaped (invisible, so search/copy work but the hidden glyph order isn't visual); full Tamil font (~340 KB) is embedded per PDF, not subset; PDF is rebuilt from the 1600px image, not the original resolution; export text is only as good as the OCR (paddle still misorders some vowel signs, e.g. `பயெனன்கொால்`).
